#!/usr/bin/env bash
set -euo pipefail

file="publish-files.log"

if [[ -f "$file" ]]; then
  echo "Deleting files listed in $file..."

  while IFS= read -r path; do
    [[ -z "$path" ]] && continue
    rm -rfv -- "$path"
  done < "$file"

  rm -f "$file"
fi